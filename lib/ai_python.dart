import 'package:flutter/material.dart';
class AIPythonScreen extends StatelessWidget {
  const AIPythonScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: Text("AI & Python")));
}

