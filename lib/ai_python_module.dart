import 'package:flutter/material.dart';

class AIModule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("بايثون والذكاء الاصطناعي"), backgroundColor: Colors.blueAccent),
      body: Center(child: Text("شرح الشبكات العصبية وكلام آلان تورينج", style: TextStyle(color: Colors.white))),
    );
  }
}
